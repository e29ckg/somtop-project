# BACKEND.md - สรุปโครงสร้างระบบ Backend (Somtop Project)

เอกสารฉบับนี้รวบรวมโครงสร้างและสถาปัตยกรรมฝั่ง Backend ของ "โปรแกรมบริหารจัดการ พ.สมทบ" เพื่อใช้เป็นคู่มือสำหรับนักพัฒนา[cite: 1]

## 1. เทคโนโลยีหลัก (Tech Stack)
* **ภาษาโปรแกรมและเซิร์ฟเวอร์**: ใช้งาน Node.js ร่วมกับเฟรมเวิร์ก Express.js เป็นเว็บเซิร์ฟเวอร์หลัก พร้อมใช้งานไลบรารี `mysql2` สำหรับเชื่อมต่อฐานข้อมูลแบบ Connection Pool
* **ฐานข้อมูล**: ใช้งาน MySQL 8.0 ควบคู่กับเครื่องมือจัดการ phpMyAdmin[cite: 1]
* **สภาพแวดล้อม**: ทำงานและเชื่อมต่อระบบทั้งหมดบนคอนเทนเนอร์ Docker ผ่านไฟล์ `docker-compose.yml`[cite: 1]

## 2. สถาปัตยกรรมระบบ (Architecture)
* **โครงสร้างแบ่งเลเยอร์ (Layered Architecture)**: แยกสัดส่วนการทำงานชัดเจนแบบ Route-Controller-Service เพื่อรองรับโปรเจกต์ขนาดใหญ่
* **การตั้งค่าเซิร์ฟเวอร์ (App/Server)**: 
  * `app.js`: ศูนย์กลางจัดการ Middleware, CORS (เปิดรับ Credentials), และรวบรวม Routes
  * `server.js`: หน้าที่โหลด `.env` และสตาร์ทเซิร์ฟเวอร์ (เปิด Port)
* **เครื่องมือช่วยเหลือ (Utils)**: รวมฟังก์ชันกลางที่ใช้บ่อย เช่น ระบบ `logger.js` สำหรับบันทึกประวัติการทำงานอัตโนมัติ

## 3. ความปลอดภัย (Security & Authentication)
* **การยืนยันตัวตน**: ใช้เทคโนโลยี JWT (JSON Web Token) เพื่อให้ระบบเบาและเป็น Stateless[cite: 1]
* **ป้องกันการถูกขโมย Token (XSS)**: ฝัง JWT ลงใน HttpOnly Cookie เพื่อป้องกันไม่ให้ JavaScript ฝั่งหน้าเว็บเข้าถึงได้[cite: 1]
* **การปกปิดข้อมูลสำคัญ**: อ่านค่า Secret Key, การเชื่อมต่อฐานข้อมูล, และโดเมนผ่านตัวแปรจากไฟล์ `.env` ทั้งหมด[cite: 1]
* **การเข้ารหัส**: ใช้ไลบรารี `bcrypt` สำหรับเข้ารหัสผ่านในฐานข้อมูล 
* **ป้องกันการปลอมแปลง (Multi-tenant)**: ดึงข้อมูล `court_code` จากการถอดรหัส Token ในฝั่ง Backend เสมอ ป้องกันการปลอมแปลงข้อมูลข้ามศาล[cite: 1]

## 4. โครงสร้างฐานข้อมูล (Database Schema)
* **`users`**: ตารางเก็บข้อมูลเข้าสู่ระบบของผู้ดูแลและเจ้าหน้าที่ (เก็บรหัสผ่านที่ถูก Hash และบทบาทสิทธิ์การใช้งาน)[cite: 1]
* **`somtop`**: ตารางหลักเก็บข้อมูลรายชื่อ, ข้อมูลส่วนตัว, และ Path ไฟล์รูปโปรไฟล์ของผู้พิพากษาสมทบ[cite: 1]
* **`leave_types`**: ตารางอ้างอิงประเภทการลาพื้นฐาน เช่น ลาป่วย, ลากิจส่วนตัว, ลาพักผ่อน[cite: 1]
* **`leave_requests`**: ตารางบันทึกประวัติใบลาและการอนุมัติ ซึ่งเชื่อมโยงข้อมูล (Foreign Key) ไปยังตาราง `somtop` และ `leave_types`[cite: 1]
* **`activity_logs`**: ตารางบันทึกประวัติการใช้งานทั้งหมดในระบบ (เพิ่ม/ลด/แก้ไข) เพื่อการตรวจสอบย้อนหลัง