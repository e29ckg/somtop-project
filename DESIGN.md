# 🎨 Design System & Guidelines
**โปรแกรมบริหารจัดการ ผู้พิพากษาสมทบ (ศาลเยาวชนและครอบครัว)**

> เอกสารนี้เป็นแหล่งอ้างอิงหลัก (single source of truth) สำหรับงาน Frontend ใหม่ทุกหน้า

เอกสารฉบับนี้รวบรวมมาตรฐานการออกแบบและแนวทางการเขียนโค้ดฝั่ง Frontend (Vue.js) เพื่อให้ระบบมีหน้าตาไปในทิศทางเดียวกัน สวยงาม และง่ายต่อการดูแลรักษา (Maintain)

---

## 1. Color Palette (โทนสีหลัก)
ระบบออกแบบโดยอ้างอิงความน่าเชื่อถือ เรียบง่าย สะอาดตา (Light Mode)

*   **Primary Color (สีหลัก):** เขียว Emerald
    *   `#10B981` (ปุ่มหลัก, เส้นขอบกล่องข้อความเมื่อ Focus, ไอคอน)
    *   `#059669` (สีเมื่อ Hover ปุ่มหลัก)
*   **Background Colors (สีพื้นหลัง):**
    *   `#F4F6F9` (สีพื้นหลังหลักของระบบ/หน้าจอ)
    *   `#FFFFFF` (สีพื้นหลังของการ์ดเนื้อหา, Modal, ตาราง)
    *   `#F9FAFB` (สีพื้นหลังช่อง Input, แถบหัวตาราง)
    *   `#F3F4F6` (สีเมื่อ Hover แถวในตาราง หรือปุ่มไอคอน)
*   **Text Colors (สีตัวอักษร):**
    *   `#111827` (สีตัวอักษรหลัก, หัวข้อสำคัญ, เน้นเข้ม)
    *   `#374151` (สีตัวอักษรเนื้อหาทั่วไป, ลาเบลแบบฟอร์ม)
    *   `#6B7280` (สีตัวอักษรรอง, ข้อความอธิบายซับไตเติล)
*   **Status Colors (สีป้ายสถานะ):**
    *   **ใช้งาน/อนุมัติ:** พื้นหลัง `#D1FAE5`, ตัวอักษร `#065F46`
    *   **พ้นสภาพ/ไม่อนุมัติ:** พื้นหลัง `#FEE2E2`, ตัวอักษร `#991B1B`
    *   **รอตรวจสอบ:** พื้นหลัง `#FEF3C7`, ตัวอักษร `#92400E`

---

## 2. Typography (ตัวอักษร)
*   **Font Family:** `'Sarabun', 'Noto Sans Thai', sans-serif` (ใช้ฟอนต์สารบรรณเป็นหลักเพื่อความเป็นทางการของหน่วยงานราชการ)
*   **ขนาดอักษรมาตรฐาน:**
    *   หัวข้อหน้า (Page Title): `24px` (Font Weight: 600)
    *   เนื้อหาทั่วไป (Body Text): `14px`
    *   ข้อความขนาดเล็ก (Small Text/Labels): `13px` หรือ `12px`

---

## 3. CSS Architecture (โครงสร้าง CSS)
ระบบได้เปลี่ยนมาใช้ **Global CSS** เป็นหลัก เพื่อลดความซ้ำซ้อนของโค้ดในแต่ละไฟล์ `.vue`

*   **ไฟล์หลัก:** `frontend/src/assets/global.css`
*   **Design tokens:** `frontend/src/assets/design-tokens.css`
*   **การอ้างอิง:** นำเข้า (Import) ไว้ที่ `frontend/src/main.js`
*   **การใช้งาน:** ในหน้า `.vue` ต่างๆ ไม่จำเป็นต้องใส่ `<style scoped>` นอกเสียจากว่ามี CSS พิเศษที่ต้องการให้มีผลแค่หน้านั้นๆ จริงๆ (เช่น รูปแบบขนาด iframe สำหรับดู PDF)
*   **กฎสำคัญ:** สี ระยะห่าง ขอบมน ความสูง control และเงาใหม่ให้ใช้ตัวแปร `var(--...)` จาก design tokens ก่อนเขียนค่าซ้ำ

---

## 4. UI Components (ส่วนประกอบหน้าจอมาตรฐาน)

### 4.1 Buttons (ปุ่มกด)
*   `.btn-primary`: ปุ่มหลักสีเขียว โค้ง 8px มีเงา (Box-shadow) เล็กน้อย
*   `.btn-secondary`: ปุ่มรองสีขาว ขอบเทา โค้ง 8px
*   `.btn-icon`: ปุ่มสี่เหลี่ยมเล็กๆ สีเทาอ่อน ใช้สำหรับไอคอนจัดการ (แก้ไข, ลบ, ดูไฟล์)

### 4.2 Tables & Cards (ตารางและการ์ด)
*   `.card`: กล่องเนื้อหาสีขาว ขอบมน `12px` มีเงาบางๆ
*   `.data-table`: ตารางกว้าง 100% เส้นขอบล่างบางๆ (ไม่ตีตารางเป็นตารางหมากรุก)
*   `.table-header-actions`: แถบเครื่องมือเหนือตาราง (แบ่งซ้าย-ขวา สำหรับเลือกจำนวนรายการ และ ค้นหา)

### 4.3 Forms & Inputs (แบบฟอร์ม)
*   `.form-grid`: แบ่งฟอร์มเป็น 2 คอลัมน์ด้วย CSS Grid (`grid-template-columns: 1fr 1fr`)
*   `.input-group`: จัดกลุ่ม Label และ Input
*   `.date-inputs`: ชุด Dropdown สำหรับเลือก วัน/เดือน/ปีเกิด แบ่ง 3 กล่องเรียงแนวนอน

### 4.4 Modals (หน้าต่างป๊อปอัป)
*   `.modal-overlay`: พื้นหลังสีดำโปร่งแสง `rgba(17,24,39,0.6)`
*   `.modal-card`: กล่องเนื้อหาสีขาว กว้างสูงสุด `600px` (หรือ `900px` สำหรับดู PDF)

### 4.5 Pagination (การแบ่งหน้า)
*   `.pagination-container`: แถบควบคุมด้านล่างตาราง ประกอบด้วยข้อความแจ้งจำนวนรายการ และปุ่มเปลี่ยนหน้า
*   `.page-btn`: ปุ่มเปลี่ยนหน้า หากเป็นหน้าที่กำลังแอคทีฟ (Active) จะเป็นปุ่มสีเขียว

### 4.6 Filter bar (แถบค้นหาและตัวกรอง)
*   ใช้ `.card.filter-card` เป็นกล่องครอบ
*   ใช้ `.filter-controls` จัดกลุ่มตัวกรอง และ `.filter-group` ครอบ label กับ input/select
*   ใช้ `.filter-actions` สำหรับปุ่มล้างค่า พิมพ์ หรือส่งออก
*   Label ต้องอยู่เหนือ control และ control ในแถวเดียวกันควรสูงเท่ากัน
*   รอบปีราชการใช้รูปแบบ `ปี 2569 (1 เม.ย. 2569 - 31 มี.ค. 2570)`

ตัวอย่าง:

```html
<section class="card filter-card no-print">
  <div class="filter-controls">
    <div class="filter-group">
      <label for="keyword">ค้นหา</label>
      <input id="keyword" type="search" placeholder="ระบุคำค้นหา..." />
    </div>
    <div class="filter-actions">
      <button class="btn-secondary">ล้างค่า</button>
      <button class="btn-primary">🖨️ พิมพ์รายงาน</button>
    </div>
  </div>
</section>
```

### 4.7 File actions (การจัดการไฟล์)
*   อัปโหลดใช้ `📤`
*   เปิดดูไฟล์แนบใช้ `📎` หรือไอคอนชนิดไฟล์
*   พิมพ์ใช้ `🖨️`
*   แก้ไขใช้ `✏️` และลบใช้ `🗑️`
*   การลบต้องมีหน้าต่างยืนยันเสมอ

### 4.8 Responsive layout
*   Breakpoint หลักคือ `768px`
*   Filter และ header actions ต้องเรียงเป็นคอลัมน์บนมือถือ
*   ตารางกว้างต้องครอบด้วย `.table-responsive`
*   ปุ่มสำคัญต้องมีพื้นที่กดไม่น้อยกว่าความสูงมาตรฐาน `--control-height`

---

## 5. System Utilities & Patterns (เครื่องมือและรูปแบบการเขียนโค้ด)

### 5.1 ระบบแจ้งเตือน (SweetAlert2)
ยกเลิกการใช้ `alert()` และ `confirm()` ธรรมดา และเปลี่ยนมาใช้ **SweetAlert2** ผ่านไฟล์ Utility ส่วนกลางที่ `frontend/src/utils/swal.js`
*   `swalSuccess(title, text)` - แจ้งเตือนเมื่อสำเร็จ (ไอคอนสีเขียว)
*   `swalError(title, text)` - แจ้งเตือนเมื่อล้มเหลว (ไอคอนกากบาทสีแดง)
*   `swalConfirm(title, text)` - กล่องข้อความยืนยัน (มีปุ่ม ยกเลิก / ดำเนินการ)

### 5.2 การจัดการวันที่ (Date Handling)
*   **รับข้อมูลฝั่งผู้ใช้:** ใช้ `<select>` แบบแบ่งแยก 3 กล่อง (วัน / เดือนไทย / ปี พ.ศ.)
*   **ส่งข้อมูลไป Backend:** แปลงกลับเป็นรูปแบบสากล `YYYY-MM-DD` (ค.ศ.) เสมอ
*   **ฟังก์ชันแสดงผล (`formatThaiDateFull`):** รับค่า `YYYY-MM-DD` แล้วแสดงในรูปแบบ `11 พฤศจิกายน 2569`

### 5.3 การพิมพ์รายงาน (Print)
*   ใช้คลาส `.no-print` กับองค์ประกอบที่ไม่ต้องการให้พิมพ์ออกมา (เช่น ปุ่มจัดการ, เมนู, ช่องค้นหา)
*   ใช้ JavaScript `window.open` เพื่อคัดลอกเฉพาะเนื้อหาตาราง (คลาส `.print-area`) ไปเปิดหน้าต่างใหม่ ทำให้กระดาษที่พิมพ์ออกมาสะอาด 100%

### 5.4 Catch-all Route (404 Page)
*   มีหน้า `NotFoundView.vue` รองรับการพิมพ์ URL ผิด (แสดงผลสวยงามและมีปุ่มกลับหน้าหลัก)
*   ตั้งค่าไว้ล่างสุดของ `router/index.js` ด้วยเส้นทาง `/:pathMatch(.*)*`
